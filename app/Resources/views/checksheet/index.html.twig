{% extends 'base.html.twig' %}

{% block body %}
    <h1>CheckSheet list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for checkSheet in checkSheets %}
            <tr>
                <td><a href="{{ path('checksheet_show', { 'id': checkSheet.id }) }}">{{ checkSheet.id }}</a></td>
                <td>{{ checkSheet.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('checksheet_show', { 'id': checkSheet.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('checksheet_edit', { 'id': checkSheet.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('checksheet_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
