{% extends 'base.html.twig' %}

{% block body %}
    <h1>CheckSheet</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ checkSheet.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ checkSheet.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('checksheet_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('checksheet_edit', { 'id': checkSheet.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
